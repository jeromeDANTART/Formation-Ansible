- name: xxx
  hosts: s1
  become: yes
  tasks:
  - name: xxx
    lineinfile:
      path: /etc/sudoers
      regexp: '^%sudo'
      line: '%sudo ALL=(ALL) NOPASSWD: ALL'
      validate: 'visudo -cf %s' # validation syntaxique du fichier /etc/sudoers